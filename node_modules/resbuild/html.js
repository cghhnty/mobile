var fs = require('fs');

module.exports = function(files, opts, cb) {
	if (!opts.srcModified())
		return cb();

	var code = '';
	files.map(function(file) {
		code += fs.readFileSync(file);
	});

	cb(null, {
		code: code
	});
};
