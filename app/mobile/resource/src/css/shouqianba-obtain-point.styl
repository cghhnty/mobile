@import 'nib';

#obtainPointRule {
  background: #ffffff;
  padding: 15px;

  .rule-section {
    margin-bottom: 10px;

    .rule-title {
      padding-bottom: 5px;
      font-size: 16px;
      border-bottom: 1px solid rgba(0,0,0,.7);
    }
      .rule-detail {
        font-size: 14px;
        margin-left: -15px;
      }
  }

}